% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform_abundance.R
\name{transform_samples}
\alias{transform_samples}
\title{Transform Samples after Sample Group Summarization}
\usage{
transform_samples(
  object,
  group_by,
  what = c("sum_intensity", "mean_intensity", "median_intensity"),
  method = c("relative", "log", "presence_absence", "clr"),
  pseudocount = 1,
  ...
)
}
\arguments{
\item{object}{A \code{microbiome_dataset} object.}

\item{group_by}{A column in \code{sample_info}.}

\item{what}{Aggregation method used before transformation.}

\item{method}{Transformation method.}

\item{pseudocount}{A pseudocount added before transformations that require
strictly positive values.}

\item{...}{Other parameters passed to \code{\link[=transform_counts]{transform_counts()}}.}
}
\value{
A transformed \code{microbiome_dataset} object.
}
\description{
Summarize samples by a grouping column in \code{sample_info} and then apply a
count transformation to the summarized abundance matrix.
}
\examples{
data("global_patterns", package = "microbiomedataset")

x <- transform_samples(
  object = global_patterns,
  group_by = "SampleType",
  what = "sum_intensity",
  method = "relative"
)
attr(x@expression_data, "transform_method")
}
